const initialState = {
    label: 'Reset Password'
}

const resetPassword = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default resetPassword
